import React, { PureComponent } from 'react';

export default class Hardware extends PureComponent {
    render(){
        return(
            <section id="hardware">
                <div className="container">
                    <h3>Not supported at this current moment</h3>
                </div>
            </section>
        )
    }
}