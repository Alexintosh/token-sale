import React, { PureComponent } from 'react';
import StepContainer            from '../contribute/StepContainer';

export default class Contribute extends PureComponent{
    render(){
        return (
            <section id="sale-contribute">
                <StepContainer />
            </section>
        )
    }
}